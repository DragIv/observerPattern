package weatherStation.badImplementation;

public class WeatherData {
    public void measurementsChanged() {
//        float temp = getTemperature();
//        float humidity = getHumidity();
//        float pressure = getPressure();
//
//        currentConditionsDisplay.update(temp, humidity, pressure);
//        statisticsDisplay.update(temp, humidity, pressure);
//        forecastDisplay.update(temp, humidity, pressure);
    }
}

// Мы программируем на уровне реализаций, а не интерфейсов

// Для каждого нового элемента придется изменять код

//Элементы не могут добавляться во время выполнения

//Переменные аспекты архитектуры не инкапсулируются
